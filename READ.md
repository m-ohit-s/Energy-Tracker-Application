# Getting Started

## How to start
First thing you need to do is run docker containers.

run the following command in root directory of the project:
```
docker compose -v up -d
```

To stop docker containers do:
```
docker compose down
```

You might have to delete pre-existing volumes if you are encountering issues with DB.