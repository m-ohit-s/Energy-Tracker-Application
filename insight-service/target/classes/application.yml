server:
  port: 8086

endpoint:
  usage-service-url: "http://localhost:8084/api/v1/usage"

spring:
  ai:
    ollama:
      base-url: "http://localhost:11434"
      chat:
        options:
          model: "tinyllama"
      init:
        pull-model-strategy: when_missing
